
describe('path', () => {
  context('#fn()', () => {
    it('Should ', () => {
      expect(1).to.equal(1);
      expect(2).to.equal(2);
    });
  });
});

describe('dog', () => {
  context('#bark()', () => {
    it('Should make noise', () => {
      expect({ hello: 'world' }).to.deep.equal({ yolo: 'for real' });
    });
  });
});
